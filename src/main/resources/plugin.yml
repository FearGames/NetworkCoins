name: ${project.name}
version: ${project.version}
main: ${project.groupId}.${project.artifactId}.${project.name}
api-version: 1.13
authors: [MinecraftJoshjr, sgdc3]
description: ${project.description}
